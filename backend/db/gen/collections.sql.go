// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: collections.sql

package db

import (
	"context"
	"encoding/json"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const getInvoiceCollectionState = `-- name: GetInvoiceCollectionState :one
SELECT invoice_id, risk_score, engagement_state, next_best_action, recommended_tone, recommended_send_at, reasons, metrics, last_event_at, last_evaluated_at, applied_at, created_at, updated_at FROM invoice_collection_states
WHERE invoice_id = $1
`

func (q *Queries) GetInvoiceCollectionState(ctx context.Context, invoiceID uuid.UUID) (InvoiceCollectionState, error) {
	row := q.db.QueryRow(ctx, getInvoiceCollectionState, invoiceID)
	var i InvoiceCollectionState
	err := row.Scan(
		&i.InvoiceID,
		&i.RiskScore,
		&i.EngagementState,
		&i.NextBestAction,
		&i.RecommendedTone,
		&i.RecommendedSendAt,
		&i.Reasons,
		&i.Metrics,
		&i.LastEventAt,
		&i.LastEvaluatedAt,
		&i.AppliedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const markInvoiceCollectionStateApplied = `-- name: MarkInvoiceCollectionStateApplied :one
UPDATE invoice_collection_states
SET applied_at = NOW(), updated_at = NOW()
WHERE invoice_id = $1
RETURNING invoice_id, risk_score, engagement_state, next_best_action, recommended_tone, recommended_send_at, reasons, metrics, last_event_at, last_evaluated_at, applied_at, created_at, updated_at
`

func (q *Queries) MarkInvoiceCollectionStateApplied(ctx context.Context, invoiceID uuid.UUID) (InvoiceCollectionState, error) {
	row := q.db.QueryRow(ctx, markInvoiceCollectionStateApplied, invoiceID)
	var i InvoiceCollectionState
	err := row.Scan(
		&i.InvoiceID,
		&i.RiskScore,
		&i.EngagementState,
		&i.NextBestAction,
		&i.RecommendedTone,
		&i.RecommendedSendAt,
		&i.Reasons,
		&i.Metrics,
		&i.LastEventAt,
		&i.LastEvaluatedAt,
		&i.AppliedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertInvoiceCollectionState = `-- name: UpsertInvoiceCollectionState :one
INSERT INTO invoice_collection_states (
    invoice_id,
    risk_score,
    engagement_state,
    next_best_action,
    recommended_tone,
    recommended_send_at,
    reasons,
    metrics,
    last_event_at,
    last_evaluated_at,
    applied_at,
    updated_at
)
VALUES (
    $1,
    $2,
    $3,
    $4,
    $5,
    $6,
    $7,
    $8,
    $9,
    NOW(),
    $10,
    NOW()
)
ON CONFLICT (invoice_id) DO UPDATE SET
    risk_score          = EXCLUDED.risk_score,
    engagement_state    = EXCLUDED.engagement_state,
    next_best_action    = EXCLUDED.next_best_action,
    recommended_tone    = EXCLUDED.recommended_tone,
    recommended_send_at = EXCLUDED.recommended_send_at,
    reasons             = EXCLUDED.reasons,
    metrics             = EXCLUDED.metrics,
    last_event_at       = EXCLUDED.last_event_at,
    last_evaluated_at   = NOW(),
    applied_at          = EXCLUDED.applied_at,
    updated_at          = NOW()
RETURNING invoice_id, risk_score, engagement_state, next_best_action, recommended_tone, recommended_send_at, reasons, metrics, last_event_at, last_evaluated_at, applied_at, created_at, updated_at
`

type UpsertInvoiceCollectionStateParams struct {
	InvoiceID         uuid.UUID          `json:"invoice_id"`
	RiskScore         int32              `json:"risk_score"`
	EngagementState   string             `json:"engagement_state"`
	NextBestAction    string             `json:"next_best_action"`
	RecommendedTone   string             `json:"recommended_tone"`
	RecommendedSendAt pgtype.Timestamptz `json:"recommended_send_at"`
	Reasons           json.RawMessage    `json:"reasons"`
	Metrics           json.RawMessage    `json:"metrics"`
	LastEventAt       pgtype.Timestamptz `json:"last_event_at"`
	AppliedAt         pgtype.Timestamptz `json:"applied_at"`
}

func (q *Queries) UpsertInvoiceCollectionState(ctx context.Context, arg UpsertInvoiceCollectionStateParams) (InvoiceCollectionState, error) {
	row := q.db.QueryRow(ctx, upsertInvoiceCollectionState,
		arg.InvoiceID,
		arg.RiskScore,
		arg.EngagementState,
		arg.NextBestAction,
		arg.RecommendedTone,
		arg.RecommendedSendAt,
		arg.Reasons,
		arg.Metrics,
		arg.LastEventAt,
		arg.AppliedAt,
	)
	var i InvoiceCollectionState
	err := row.Scan(
		&i.InvoiceID,
		&i.RiskScore,
		&i.EngagementState,
		&i.NextBestAction,
		&i.RecommendedTone,
		&i.RecommendedSendAt,
		&i.Reasons,
		&i.Metrics,
		&i.LastEventAt,
		&i.LastEvaluatedAt,
		&i.AppliedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
